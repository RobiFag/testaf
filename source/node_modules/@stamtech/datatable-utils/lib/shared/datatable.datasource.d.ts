import { CollectionViewer, DataSource } from "@angular/cdk/collections";
import { Observable } from "rxjs";
import { FilterSet } from "./datatable.types";
import { DatatableService } from "../services/datatable.service.interface";
export declare class DatatableDatasource<T> implements DataSource<T> {
    private rowsService;
    private rows;
    private loadingSubject;
    loading$: Observable<boolean>;
    private countSubject;
    counter$: Observable<number>;
    constructor(rowsService: DatatableService<T>);
    connect(collectionViewer: CollectionViewer): Observable<T[]>;
    disconnect(collectionViewer: CollectionViewer): void;
    loadData(filterSet: FilterSet): void;
}

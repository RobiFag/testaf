import { ColumnFilterService } from '../../services/column-filter.service';
import { Filter } from '../../shared/datatable.types';
export declare class ChipsFilterComponent {
    private filtersService;
    chips: Filter[];
    constructor(filtersService: ColumnFilterService);
    clear(key: string): void;
    clearFilters(): void;
}

import { FormGroup } from '@angular/forms';
import { MatDialogRef } from '@angular/material/dialog';
import { ColumnFilterService } from '../../services/column-filter.service';
import { FilterSet } from '../../shared/datatable.types';
export declare class DialogFilterComponent {
    private dialog;
    data: any;
    filterSet: FilterSet;
    filtersForm: FormGroup;
    filtersService: ColumnFilterService;
    constructor(dialog: MatDialogRef<DialogFilterComponent>, data: any);
    clearFilters(): void;
    search(): void;
}
